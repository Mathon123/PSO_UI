"""
常量和样式配置
==============

【配置文件说明】
本文件包含应用程序的所有常量定义，包括：
1. 应用程序基本信息
2. 窗口尺寸设置
3. 配色方案（Figma风格）
4. 字体设置
5. 间距和圆角规范
6. 支持的文件类型
7. PSO默认参数

【设计规范参考】
- 窗口最小尺寸: 1400x900 (适合大多数笔记本)
- 默认窗口尺寸: 1700x1050 (适合1920x1080显示器)
- 主色调: #4A90D9 (蓝色)
- 圆角: 4px(小), 8px(中), 12px(大)

【字号规范】
- size_xs: 10px (辅助说明)
- size_sm: 11px (次要标签)
- size_md: 12px (默认正文)
- size_lg: 14px (小标题)
- size_xl: 16px (大标题)
- size_xxl: 18px (数值显示)
"""

# =============================================================
# 应用程序信息
# =============================================================
# APP_NAME: 应用程序显示名称
APP_NAME = "PSO数据分析工具"

# APP_VERSION: 版本号
APP_VERSION = "1.0.0"

# =============================================================
# 窗口默认尺寸
# 【设计原则】
# - 最小尺寸: 防止布局过于紧凑
# - 默认尺寸: 适合主流显示器
# =============================================================

# WINDOW_MIN_WIDTH: 窗口最小宽度 1400px
# 原因: 左侧面板260px + 右侧Tab区域1100px + 边距
WINDOW_MIN_WIDTH = 1400

# WINDOW_MIN_HEIGHT: 窗口最小高度 900px
# 原因: 工具栏约40px + 菜单栏约25px + 内容区 + 状态栏约25px
WINDOW_MIN_HEIGHT = 900

# WINDOW_DEFAULT_WIDTH: 默认宽度 1700px
# 适合1920x1080显示器，留有边距
WINDOW_DEFAULT_WIDTH = 1700

# WINDOW_DEFAULT_HEIGHT: 默认高度 1050px
# 适合1080p显示器
WINDOW_DEFAULT_HEIGHT = 1050

# =============================================================
# Figma风格配色方案 - 浅色主题
# 【配色原则】
# - 主色调用于按钮、重点强调
# - 背景色区分不同层级
# - 文字颜色区分重要程度
# - 边框色统一视觉风格
# =============================================================
COLORS = {
    # -------- 主色调 --------
    # primary: 主按钮背景、高亮文字
    "primary": "#4A90D9",         # 蓝色 RGB(74, 144, 217)
    
    # primary_hover: 主按钮悬停状态
    "primary_hover": "#3A7BC8",   # 稍深蓝色
    
    # primary_pressed: 主按钮按下状态
    "primary_pressed": "#2D6BB0",  # 更深蓝色

    # -------- 背景色 --------
    # bg_main: 主内容区背景
    "bg_main": "#F8F9FA",         # 浅灰色 RGB(248, 249, 250)
    
    # bg_card: 卡片、面板背景
    "bg_card": "#FFFFFF",         # 纯白色
    
    # bg_sidebar: 侧边栏背景
    "bg_sidebar": "#FFFFFF",      # 纯白色
    
    # bg_hover: 悬停状态背景
    "bg_hover": "#EEF2F7",       # 淡蓝灰色

    # -------- 文字颜色 --------
    # text_primary: 主要文字、标题
    "text_primary": "#2C3E50",    # 深灰蓝色 RGB(44, 62, 80)
    
    # text_secondary: 次要文字、标签
    "text_secondary": "#7F8C8D", # 中灰色 RGB(127, 140, 141)
    
    # text_muted: 辅助说明文字
    "text_muted": "#95A5A6",      # 浅灰色 RGB(149, 165, 166)

    # -------- 边框和分割线 --------
    # border: 默认边框颜色
    "border": "#E1E8ED",          # 淡灰 RGB(225, 232, 237)
    
    # border_light: 浅色边框
    "border_light": "#F0F3F6",    # 更浅灰
    
    # divider: 分割线
    "divider": "#DEE2E6",         # 分割灰

    # -------- 状态颜色 --------
    # success: 成功状态
    "success": "#27AE60",         # 绿色 RGB(39, 174, 96)
    
    # warning: 警告状态
    "warning": "#F39C12",          # 橙色 RGB(243, 156, 18)
    
    # danger: 错误状态
    "danger": "#E74C3C",           # 红色 RGB(231, 76, 60)
    
    # info: 信息提示
    "info": "#3498DB",            # 蓝色

    # -------- 图表颜色 --------
    # 用于图表中的多条数据线/柱状图配色
    # 使用彩虹色谱，确保多数据对比时区分度高
    "chart_colors": [
        "#4A90D9",  # 蓝色 - 第1条曲线（主色）
        "#E74C3C",  # 红色 - 第2条曲线
        "#2ECC71",  # 绿色 - 第3条曲线
        "#9B59B6",  # 紫色 - 第4条曲线
        "#F39C12",  # 橙色 - 第5条曲线
        "#1ABC9C",  # 青色 - 第6条曲线
        "#E91E63",  # 粉色 - 第7条曲线
        "#00BCD4",  # 浅蓝 - 第8条曲线
    ],

    # 渐变色方案（用于热图等）
    "gradient_colors": {
        "RdYlGn": ["#D73027", "#FC8D59", "#FEE08B", "#D9EF8B", "#91CF60", "#1A9850"],
        "YlOrRd": ["#FFFFB2", "#FED976", "#FEB24C", "#FD8D3C", "#FC4E2A", "#BD0026"],
        "Blues": ["#F7FBFF", "#DEEBF7", "#C6DBEF", "#9ECAE1", "#4292C6", "#2171B5"],
    },

    # -------- 表格颜色 --------
    # table_header_bg: 表头背景色
    "table_header_bg": "#F1F3F5",  # 淡灰
    
    # table_odd_row: 奇数行背景
    "table_odd_row": "#FFFFFF",    # 白色
    
    # table_even_row: 偶数行背景（斑马纹）
    "table_even_row": "#FAFBFC",  # 极浅灰
    
    # table_selected: 选中行背景
    "table_selected": "#E3F2FD",   # 浅蓝色
}

# =============================================================
# 字体设置
# 【字体选择原则】
# - 优先系统默认字体（最佳渲染效果）
# - 中文优先使用微软雅黑
# - 回退到通用无衬线字体
# =============================================================
FONTS = {
    # family: 默认字体栈
    # Segoe UI: Windows默认界面字体
    # Microsoft YaHei: 微软雅黑，支持中文
    # sans-serif: 通用无衬线字体（最后备选）
    "family": "Segoe UI, Microsoft YaHei, sans-serif",
    
    # monospace: 等宽字体（用于代码、数值）
    "monospace": "Consolas, Courier New, monospace",

    # -------- 字号定义 --------
    # 字号梯度: xs(10) -> sm(11) -> md(12) -> lg(14) -> xl(16) -> xxl(18)
    # 每个级别递增2px，形成清晰的视觉层次
    
    # size_xs: 10px - 辅助说明、小标签
    "size_xs": 10,
    
    # size_sm: 11px - 次要标签、表格内容
    "size_sm": 11,
    
    # size_md: 12px - 默认正文
    "size_md": 12,
    
    # size_lg: 14px - 小标题
    "size_lg": 14,
    
    # size_xl: 16px - 大标题
    "size_xl": 16,
    
    # size_xxl: 18px - 数值显示
    "size_xxl": 18,
}

# =============================================================
# 间距规范
# 【间距梯度】
# 使用4px为基准的倍数，便于对齐和计算
# xs(4) -> sm(8) -> md(12) -> lg(16) -> xl(24) -> xxl(32)
# =============================================================
SPACING = {
    "xs": 4,    # 微间距 - 紧凑布局
    "sm": 8,    # 小间距 - 控件内间距
    "md": 12,   # 中间距 - 默认间距
    "lg": 16,   # 大间距 - 分组间距
    "xl": 24,   # 特大间距 - 区块间距
    "xxl": 32,  # 超大间距 - 页面边距
}

# =============================================================
# 圆角规范
# 【圆角使用场景】
# - sm(4px): 输入框、小按钮
# - md(8px): 中等卡片、较大按钮
# - lg(12px): 大卡片、模态框
# - xl(16px): 特殊强调卡片
# =============================================================
BORDER_RADIUS = {
    "sm": 4,    # 小圆角 - 4px
    "md": 8,    # 中圆角 - 8px
    "lg": 12,   # 大圆角 - 12px
    "xl": 16,   # 超大圆角 - 16px
}

# =============================================================
# 数据文件类型
# 【支持格式】
# - Excel: .xlsx, .xls (结构化表格数据)
# - CSV: .csv (通用数据格式)
# - JSON: .json (结构化配置)
# - 文本: .txt (日志、说明)
# =============================================================
SUPPORTED_FILE_TYPES = {
    "Excel": ["*.xlsx", "*.xls"],
    "CSV": ["*.csv"],
    "JSON": ["*.json"],
    "文本": ["*.txt"],
}

# =============================================================
# 数据类型配置
# 【数据类型说明】
# - nerve: 胫神经电刺激数据（多频率响应曲线）
# - generic: 通用数据
# =============================================================
DATA_TYPES = {
    "nerve": {
        "name": "胫神经电刺激数据",
        "description": "多频率响应曲线数据",
        # 标准列名：电流(mA)、10Hz频率响应、20Hz频率响应
        "columns": ["电流_mA", "Freq_10Hz", "Freq_20Hz", "响应概率"],
    },
    "generic": {
        "name": "通用数据",
        "description": "通用表格数据",
    },
}

# =============================================================
# 图表类型
# 【图表分类】
# - response: 电流-响应概率曲线
# - convergence: PSO收敛曲线
# - residual: 残差分析
# - bar: 参数对比柱状图
# - scatter: 散点图
# - heatmap: 热力图
# =============================================================
CHART_TYPES = {
    "response": "响应曲线",
    "convergence": "收敛曲线",
    "residual": "残差分析",
    "bar": "参数柱状图",
    "scatter": "散点图",
    "heatmap": "热力图",
}

# =============================================================
# PSO默认配置
# 【参数说明】
# - n_particles: 粒子数，影响搜索能力和速度
# - n_iterations: 最大迭代次数
# - c1: 认知系数（个体最优影响力）
# - c2: 社会系数（全局最优影响力）
# - w_max/w_min: 惯性权重范围（平衡探索与开发）
# - target_rmse: 优化目标
# =============================================================
PSO_DEFAULT_CONFIG = {
    "n_particles": 100,       # 粒子数: 100个粒子
    "n_iterations": 50,       # 迭代次数: 50代
    "c1": 1.8,               # 认知系数: 1.8
    "c2": 1.5,               # 社会系数: 1.5
    "w_max": 0.9,             # 最大惯性权重: 0.9
    "w_min": 0.4,             # 最小惯性权重: 0.4
    "target_rmse": 0.03,      # 目标RMSE: 0.03
}

# =============================================================
# 统计分析指标
# 【指标定义】
# - mean: 均值
# - std: 标准差
# - min/max: 极值
# - median: 中位数
# - rmse: 均方根误差
# - mae: 平均绝对误差
# - r2: 决定系数
# =============================================================
STAT_METRICS = {
    "mean": "均值",
    "std": "标准差",
    "min": "最小值",
    "max": "最大值",
    "median": "中位数",
    "rmse": "RMSE",
    "mae": "MAE",
    "r2": "R²",
}
